apply plugin: 'com.android.application'
apply plugin: 'org.jetbrains.kotlin.android'

def gitRepoRoot = rootProject.projectDir.parentFile

def gitOutput(List<String> args) {
    try {
        def proc = (['git', '-C', gitRepoRoot.absolutePath] + args).execute()
        proc.waitFor()
        return proc.text.trim()
    } catch (Exception ignored) {
        return ""
    }
}

def gitVersionCode = {
    def n = gitOutput(['rev-list', '--count', 'HEAD'])
    return n.isInteger() ? n.toInteger() : 1
}()

def gitVersionName = {
    def desc = gitOutput(['describe', '--tags', '--always'])
    if (desc.isEmpty()) desc = "dev"
    return "${gitVersionCode} - ${desc}".toString()
}()

android {
    namespace 'com.hairclinic.app'
    compileSdk 34

    defaultConfig {
        applicationId "com.hairclinic.app"
        minSdk 24
        targetSdk 34
        versionCode gitVersionCode
        versionName gitVersionName
        buildConfigField "String", "BASE_URL", '"http://8.145.53.212:8000/"'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            // 内部分发：无正式签名时使用 debug 证书
            signingConfig signingConfigs.debug
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
    buildFeatures {
        viewBinding true
        buildConfig true
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.4'
    implementation 'androidx.lifecycle:lifecycle-viewmodel-ktx:2.8.4'
    implementation 'androidx.navigation:navigation-fragment-ktx:2.7.7'
    implementation 'androidx.navigation:navigation-ui-ktx:2.7.7'
    implementation 'com.squareup.retrofit2:retrofit:2.11.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.11.0'
    implementation 'com.squareup.okhttp3:logging-interceptor:4.12.0'
}
